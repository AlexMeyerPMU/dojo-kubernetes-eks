apiVersion: v1
kind: Service
metadata:
  name: {{ include "guestbook-redis.fullname" . }}
  labels:
    {{- include "guestbook-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "guestbook-redis.selectorLabels" . | nindent 4 }}
